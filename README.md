# user-profile-manager
Structured the app using JavaScript's Module Pattern, storing all user data and logic inside a single object (userManager) to keep the code organized and easy to manage. Created a system to dynamically generate user profile cards using JavaScript DOM methods,resulting in cleaner  code. Styled the interface with a custom dark theme using CSS.
